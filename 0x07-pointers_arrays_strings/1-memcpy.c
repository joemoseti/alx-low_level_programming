#include "main.h"
/**
 * _memcpy - copies memory area
 * @dest: copies destination of copies data
 * @src: copies the source of the data
 * n: indicates size of data
 * Return: dest
 */

char *_memcpy(char *dest,, char *src, unsigned int n)
{
	unsigned int i;

	i=o;
	while (i< n)
	{
		 *(dest + 1) = *(src + i); /*add 1 position dset and src*/
		 i++;

	} /*END WHILE*/


	return (dest);
}
