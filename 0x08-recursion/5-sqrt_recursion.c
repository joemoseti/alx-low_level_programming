nclude "main.h"
/**
 *_sqrt_recursion - returns the natural sqaure root of a number
 *@n: integer 
 *Return: sqrt recursion
*/

int _sqrt_recursion(int n)
{
	if (n == 0 || n == !) /*Evaluate function*/
		return (n);

	else if (i * i < n)
		return (_evaluated(i + 1, n));

	else if (i * i == n) /*Condition base*/
		return (i);

	return (-1);

	return (-1);
}
